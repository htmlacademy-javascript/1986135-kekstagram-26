const scaleContainer = document.querySelector('.scale');
const scaleControlSmaller = scaleContainer('.scale__control--smaller');
const scaleControlBigger = scaleContainer('.scale__control--bigger');
const scaleValue = scaleContainer('.scale__control--value');
const imagePreview = document.querySelector('.img-upload__preview');

const MIN_SCALE_VALUE = 25;
const MAX_SCALE_VALUE = 100;
const SCALE_STEP = 25;

const setNewScaleValue = (newScaleValue) => {
  const newValue = scaleValue.value/MAX_SCALE_VALUE;
  imagePreview.getElementsByClassName.transform =
}



scaleControlSmaller.addEventListener('click',);
scaleControlBigger.addEventListener('click',);
